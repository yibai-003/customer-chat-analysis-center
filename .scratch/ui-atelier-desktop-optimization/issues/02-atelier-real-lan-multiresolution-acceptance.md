# 02: ATELIER 真实局域网多分辨率验收与缺陷闭环

**What to build:** 使用真实局域网登录、真实任务数据和真实权限角色，对第一阶段 UI 实现进行多分辨率验收；对验收发现的布局、可读性、状态反馈和交互缺陷在本工单内修复并留下证据。

**Blocked by:** 01: ATELIER 桌面端 UI 实现

**Status:** in-progress (2026-09-21) — 真实任务四尺寸工作台截图已归档；登录页、弹窗交互和完整状态矩阵仍待补齐

- [x] 使用真实局域网入口完成登录，并分别抽查管理员、操作人员、复核人员或只读人员可见的页面入口；权限差异不能导致布局破坏或出现无权限操作。*（2026-09-21 用户确认五个角色已在另一台电脑完成各项功能测试且无问题）*
- [x] 在 `1366x768`、`1440x900`、`1920x1080` 和 `1024x768` 下分别验收登录页和已导入任务工作台。*（登录页四尺寸 DOM 检查已有记录；真实任务工作台四尺寸截图已于 2026-09-21 归档）*
- [ ] 在至少一份真实任务中验证长文件名、长来源字段、长模型名、长字段值、空状态、加载状态、失败状态和需复核状态。
- [ ] 在 `1024x768` 下打开任意记录，确认详情抽屉不遮挡主要操作；关闭按钮、遮罩和 Escape 均可用，关闭后焦点与记录选中状态正确。
- [ ] 在记录列表中验证单选、多选、批量解析、状态筛选和详情切换，确认 UI 优化没有削弱既有业务能力。
- [ ] 打开批量解析弹窗，确认模型容量、并发数、批次大小、Token 提示、热点问题配置和提交/取消操作在低高度桌面中均可见且不被遮挡。
- [x] 检查 `body`、工作台、记录列表、详情抽屉和弹窗不存在非预期横向滚动、内容裁切、按钮溢出或文字重叠。*（四尺寸真实工作台截图与既有 DOM 溢出检查均通过；批量解析弹窗的独立交互检查仍见下方未完成项）*
- [x] 每个验收尺寸至少保留一张截图，并记录登录角色、任务、板块、触发状态、截图路径和溢出检查结果。
- [ ] 发现问题时补充回归测试或最小化复现步骤；修复后重新执行相关测试、类型检查和构建。
- [ ] 验收证据归档后，将本工单状态改为 `done`；若仍有影响正常业务的视觉或交互问题，不得提前关闭。

**当前进展（2026-09-19）：**

- [x] 已确认可用局域网入口为 `http://172.16.20.178:8788/`，新版登录页可正常加载。
- [x] `http://192.168.230.1:8788/` 当前被入口策略拒绝，返回“仅允许本机工作台或已配置的内部入口访问此接口”，未作为验收入口。
- [x] 局域网登录页已在 `1366x768`、`1440x900`、`1920x1080`、`1024x768` 完成 DOM 尺寸和横向溢出检查，四个尺寸均通过。
- [x] 已为旧局域网容器执行备份：44 个文件、38 个引用、校验通过且无 warning。
- [x] 已构建并部署 `customer-chat-analysis-center:0.1.0-ui-atelier-20260919`，复用原数据与知识库挂载；容器健康检查通过。
- [x] 运行实例返回的 HTML、CSS、JS 已确认包含 `atelier` 主题、`detail-drawer-open` 和 `sidebar-toggle` 实现。
- [x] 根据真实工作台观察结果调整插画层级：任务标题区使用单叶标本，侧栏插画缩为低对比度角标，登录页和详情空状态保留完整拼贴。
- [x] 已构建并部署 `customer-chat-analysis-center:0.1.0-ui-atelier-20260919-r2`，容器健康检查通过；全量 89 个测试文件、673 个测试通过。
- [x] 已构建并部署 `customer-chat-analysis-center:0.1.0-ui-atelier-20260919-r3`，容器健康检查通过；真实入口返回新版主题资源。
- [x] 左侧任务侧栏恢复原始插画构图与占比，仅保留折叠态隐藏行为。
- [x] 右侧详情空状态改为低对比度单标本构图，保留详情抽屉、关闭、遮罩和 Escape 行为。
- [x] 顶部配置、模型、账号、审计、备份入口归入“管理”分组；知识库同步状态移入左侧系统状态区；无任务上传区限制为居中窄幅工作区。
- [x] `npm run typecheck`、`npm run build`、客户端专项测试和全量测试通过；全量结果为 89 个测试文件、673 个测试通过。
- [x] 已重新打开真实局域网预览并完成新版空状态 DOM 复测；`1366x768`、`1440x900`、`1920x1080`、`1024x768` 四个尺寸均无横向溢出。
- [x] `1024x768` 下未选中记录时详情区域已移出视口，确认抽屉默认关闭；当前任务为空时没有记录可用于继续做详情交互验收。
- [x] 当前局域网数据库接口不再为空：2026-09-21 真实任务工作台显示 10 条记录，四尺寸截图和跨机五角色功能证据已补齐；批量解析弹窗、详情交互和完整状态矩阵仍按上方条目继续验收。

**验收证据：**

- 真实局域网访问地址和验收时间。
- 登录角色与权限范围。
- 测试任务及板块名称，必要时记录数据脱敏说明。
- 四个尺寸的截图路径。
- 横向溢出检查结果。
- 发现的问题、修复提交或文件位置、复测结果。

**新增真实工作台证据（2026-09-21）：**

| 视口 | 截图 |
| --- | --- |
| `1024x768` | `evidence/2026-09-21/workbench-1024x768.png` |
| `1366x768` | `evidence/2026-09-21/workbench-1366x768.png` |
| `1440x900` | `evidence/2026-09-21/workbench-1440x900.png` |
| `1920x1080` | `evidence/2026-09-21/workbench-1920x1080.png` |

- 用户确认四种分辨率下工作台内容完整，当前未发现横向溢出、内容裁切、按钮重叠或文字遮挡。

**新增自动化交互回归证据（2026-09-21）：**

- 详情抽屉的模态语义、Escape 关闭、焦点恢复/循环，记录选择与分页筛选竞态，批量解析容量/提交/轮询/暂停/取消，以及模型池前端选择和额度状态共 88 项测试通过。
- 登录、会话失效、角色能力控制和进度状态共 18 项测试通过。
- 完整结果见 `evidence/interaction-regression-2026-09-21.json`；真实任务状态矩阵、低分辨率浏览器操作和批量弹窗现场截图仍未据此签署。
