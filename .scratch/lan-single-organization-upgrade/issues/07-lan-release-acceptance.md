# 07: 局域网发布验收

**What to build:** 在固定局域网主机和另一台局域网工作站上完成正式发布前验收，证明身份授权、共享数据、审计、镜像持久化、内部 HTTPS、备份恢复和质量闸门共同成立。

**Blocked by:** 02: 任务操作与审核权限闭环; 03: 配置、模型池与账号管理权限及审计; 04: 局域网镜像与单实例运行单元; 05: CI 质量闸门与现有基线清理; 06: 备份恢复、内部 HTTPS 与运维手册

**Status:** in-review (2026-09-21) — 真实跨机业务闭环和五角色功能抽查已通过（当前为 HTTP 局域网入口）；正式 HTTPS 与 CI 工件追溯待办

- [ ] 从另一台局域网工作站通过内部 HTTPS 登录，验证管理员、配置人员、操作人员、审核人员和只读人员的完整权限矩阵。*（2026-09-21 用户确认五个角色已在另一台电脑完成各项功能测试且无问题；当前入口仍为 HTTP，正式 HTTPS 验收待执行）*
- [x] 验证组织内共享任务、记录、图片和结果可读取，且所有未授权写入、导出、配置和账号管理请求均在服务端拒绝。
- [x] 验证导入、解析、复核、导出、模型池/配置变更和账号操作产生准确审计记录，不泄漏凭据。
- [x] 验证容器重启与镜像升级预演后持久数据仍可用，且单实例锁阻止同一数据库的并发应用实例。
- [x] 完成备份、独立目录恢复和回滚预演，恢复后的数据、文件、知识配置和密钥验证通过。
- [ ] CI 的安装、测试、类型、lint、构建、迁移检查和冒烟检查均通过；发布使用的镜像版本可追溯到通过 CI 的提交。*（GitHub Actions 已在真实 runner 通过全部门禁；运行镜像仍为本地构建，工件追溯由工单 03 补齐）*
- [x] 记录主机规格、数据卷路径、TLS/网段配置、恢复证据、已知限制和下一阶段线上化前置条件。

**验收结果（2026-09-17）：**
- 新增 `scripts/verify-lan-acceptance.ps1` 与 `scripts/lan-acceptance-client.mjs`：构建镜像 → 首次启动环境变量引导管理员 → nginx 内网 HTTPS（自签名、保留 Host）→ 五角色完整权限矩阵与拒绝 → 导入/解析/复核/导出/配置/模型池/账号/审计 → API 备份 → 独立目录恢复与 `restore:verify` → 升级预演 → 回滚预演 → 单实例保护。
- 实测通过：客户端 14/14 步骤（4.3s）、51 条审计事件（18 类动作、25 条拒绝、无凭据泄漏）、备份 5 文件/3 引用、恢复校验 5/5（数据库版本 17、引用文件、知识哈希、`models:1` 密钥可解密）、升级与回滚后任务数据保留、单实例锁拒绝第二实例。
- 主机与镜像证据、TLS/网段配置、残留风险与人工待办清单见 `docs/archive/lan-release-acceptance-2026-09-17.md`。

**新增跨机验收证据（2026-09-21）：**

- 用户确认管理员、配置人员、操作人员、审核人员和只读人员已在另一台电脑完成各项功能测试，当前未发现功能问题。
- 本证据覆盖 HTTP 局域网业务与角色功能使用；不替代正式 HTTPS、可信证书和 CI 发布工件追溯验收。
- 2026-09-21 当前提交 `8686c5e` 的 CI 运行 `35562060219` 已完成，质量与发布 job 均成功；工件 id `10622367767`、摘要和镜像标签规则见 `.scratch/lan-single-organization-upgrade/evidence/ci-2026-09-21.json`。正式 HTTPS 和可部署批准镜像的主机现场证据仍待执行。
