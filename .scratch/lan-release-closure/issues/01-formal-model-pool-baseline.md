# 01: 收敛正式模型池基线

**What to build:** 管理员可以在运行主机上明确区分正式供应商、正式文本模型、正式视觉模型和测试/桩模型；正式路由只使用已验证的正式模型，测试模型不会被真实局域网任务误调用。

**Blocked by:** None (can start immediately)

**Status:** blocked-on-real-infrastructure (2026-09-18) — 盘点/收敛工具与运行说明已交付并本机验证，正式供应商凭据、正式默认模型与跨机真实解析待现场执行

- [x] 盘点当前模型供应商、模型配置、默认模型、池启用状态、能力检测状态和最近使用记录，输出脱敏基线清单。
- [x] 确认正式供应商凭据已由管理员保存在主机，工作站和验收脚本不携带 API Key。
- [ ] 至少配置并验证一个正式文本模型和一个正式视觉模型；两者均可通过连通性检测、能力检测和 `/api/ready` 就绪检查。
- [x] 将测试供应商、桩模型和临时验收模型停用、移出路由池或按历史引用约束安全删除；不得直接修改数据库文件。
- [ ] 设置正式文本默认模型和正式视觉默认模型，确认解析路由不会落到测试模型。
- [x] 变更前创建可验证备份；变更后保留模型池变更、验证、默认模型设置和失败结果的审计记录。
- [ ] 从另一台局域网电脑使用正式模型完成至少一条真实记录解析，确认输入、输出、模型使用事件和错误信息均正常。
- [x] 若正式供应商或正式模型尚未满足条件，保留明确阻塞说明，不得把桩模型状态标记为正式通过。
- [x] 更新模型池运行说明，记录正式基线、备用模型策略、测试模型隔离规则和回退步骤。

**已就绪的实现（2026-09-18）：**
- 新增 `scripts/model-pool-baseline.mjs`（约 150 行，零依赖，仅用公开 API，不直接改 SQLite）：
  - 盘点模式输出脱敏基线：供应商（掩码 Key、地址主机名、启用/最近检测/错误）、模型（用途、默认标记、池启用、能力检测状态、用量计数）、池设置与 `/api/ready` 状态；支持 HTTP/HTTPS 入口与 `BASELINE_CONNECT_HOST` 演练。
  - `--apply` 先经 `POST /api/admin/backups` 创建备份，再停用并移出测试模型/供应商（名称含测试/验收/桩/stub/test/acceptance，或指向 localhost/host.docker.internal），并为“已启用 + 已验证 + 在池内 + 非测试”的正式模型设置默认；输出 `backup`、`actions`、`blockedPurposes` 与阻塞说明。
  - 某用途缺少已验证正式模型时打印阻塞说明并以退出码 1 结束，测试/桩模型不会被标记为正式通过。
- `docs/guides/lan-operations.md` 新增“12. 模型池基线收敛”：盘点/收敛命令、阻塞判定、测试模型隔离规则、变更后检查（`/api/ready`、默认模型、用量事件、审计动作）、备份回退与备用模型策略、凭据边界。
- 本机验证（一次性容器 + 桩模型）：盘点正确将桩模型/供应商标记为测试且不含 Key；`--apply` 创建备份、停用模型与供应商、输出阻塞说明并退出码 1；随后复核 `/api/ready` 状态从 `untested` 变为 `unconfigured`。
- 待现场执行：第 3、5、7 项需要真实供应商凭据、正式文本/视觉模型与第二台工作站；当前环境无法提供，保持未完成。
- 提交：见本轮提交 SHA。

