# 业务术语

## 导入任务

一次 Excel 导入所形成的任务。导入时选定一个解析板块、一个数据平台和该板块当时的已发布配置版本；这些选择绑定该任务，导出只包含该板块的结果。后续发布新配置或重新启用旧版本，不自动改变已有任务使用的版本。

系统升级时，现有任务按其已绑定板块关联到该板块的 `V1`。历史任务不自动推断平台，也不自动生成会话 ID，仍可查看并按原机制导出。需要重新分析和生成会话 ID 时，由管理员先补选任务平台；补选后平台不可更改。

## 板块配置版本

所有解析板块各自维护已发布配置版本；接待流程质检是首个使用新版本的板块。版本固定字段、表头、导出设置、提示词、依赖关系、知识库内容，以及适用于该板块的业务规则。接待流程质检的业务规则包括问题目录、维度、扣分、是否直接判 D 级和等级阈值。后续配置或知识条目修改不改变已有任务的分析依据。旧版本可以重新启用供后续任务使用；已有任务继续使用导入时绑定的版本，既有结果不会因切换版本而自动重算。

每个板块只能有一个当前启用版本。导入人员选择板块和任务平台后，系统自动绑定该板块的当前启用版本，并展示版本号和版本说明；导入人员不能为单次任务临时改选版本。管理员把历史版本重新设为当前版本时，只影响之后导入的任务。

板块配置使用“草稿—发布”流程。已发布版本不可直接修改；管理员编辑草稿，发布前校验字段依赖、Excel 表头、提示词、知识库和业务规则，发布后生成板块内递增的版本号。只有已发布版本可以成为当前启用版本。系统升级时将各板块现有配置登记为 `V1`，不要求人工重新配置。

已发布版本永久保留，不允许删除或覆盖。不再使用的版本可以归档；归档版本不能直接设为当前版本，管理员恢复归档后才可重新启用。已绑定历史任务的版本始终可查看。草稿可以删除或重建，不影响已发布版本和现有任务。

## 任务平台

导入时从可维护的平台字典选定的平台，是该任务的平台权威值。源 Excel 可以没有“平台”列；若存在非空平台值且与所选平台冲突，应阻止导入并提示冲突行。导出时缺失的平台值由任务平台补齐。

平台名称可以修改，平台代码必须全局唯一。平台一旦被任务使用，其代码不可修改，只能停用；需要新代码时应新增平台配置。停用平台不影响历史任务，但不能再被新导入任务选择。

## 会话 ID

所有解析板块共用的会话标识，不是某个板块的分析字段。导入时绑定的任务平台决定其平台代码；单条记录分析达到“已完成”或“待复核”后才派发，格式为平台代码、派发当天的年月日和六位大写英文字母或数字随机码。已派发的 ID 在重试中保持不变，不同会话的 ID 必须唯一。

## 板块导出模板

每个解析板块独立定义其导出字段与行形态。全项目仅统一“平台”和“会话 ID”；其他列不作为跨板块的固定字段。一次任务只导出其导入时绑定板块的结果。

“平台”和“会话 ID”是共享业务字段，但它们在 Excel 中的目标列名由板块配置版本明确映射。接待流程质检映射为 `平台 (platform_name)` 和 `会话ID (conversation_id)`。源工作表缺少目标列时，导出在原表末尾追加；目标列存在时原位回填。

## 接待质检问题明细回填

接待流程质检的新样式首先是导入 Excel 的表头格式。导出保留原工作簿的行，只在带聊天截图的原始行回填结果；同一会话的多个问题写入同一单元格，不使用截图后方的预留行，也不因问题数增加新行。

同一会话存在多个问题时，仍只在截图行回填。“问题”“维度”“扣分”三列按相同顺序用英文逗号分隔；“合计扣分”列填写各问题扣分的总和。“是否 D 级”按会话聚合：有问题且任一问题是 D 级时填“是”，有问题但都不是 D 级时填“否”，没有问题时留空。“证据说明”和“判定理由”按问题顺序以 `1.`、`2.` 编号换行；“聊天原文”只摘录与已识别问题有关的原文，重复片段去重后合并在同一个单元格，不逐问题编号。

没有识别出问题时，“聊天原文”留空。

没有识别出问题时，“优化建议”留空，不输出固定的合规提示语。

同一会话存在多个问题时，“优化建议”按问题顺序使用 `1.`、`2.` 编号换行，并对内容相同的建议去重。

没有识别出问题且信息充分时，等级填 `A`，合计扣分填 `0`，是否待人工复核填“否”；维度、问题、扣分、是否 D 级、聊天原文、证据说明、判定理由和优化建议全部留空。存在会话开始时间无法可靠识别等需要复核的缺失信息时，是否待人工复核填“是”，但不得因此虚构问题。

“会话开始时间”仅在截图中能可靠识别完整日期和时间时填写；否则留空并标记待人工复核，不使用业务日期或导入时间推算。

“对话轮数”按客户和人工客服的一次往返计一轮，不按单条消息数量计数。客户连续发送多条消息后客服回复一次，算一轮；会话末尾只有客户消息、尚未收到客服回复时，不算完整的一轮。机器人和系统消息不算人工客服回复。

接待质检分析采用两阶段识别和本地派生：视觉模型先从截图抽取客观对话事实；统一质检模型再从当前版本的问题目录中选择问题 ID，并输出对应原文、证据说明和判定理由；维度、扣分、D 级、总扣分、等级、建议和是否待复核由任务绑定版本中的结构化规则本地生成。不能仅凭截图确认的规则不得强行判定。

接待流程质检新版本以当前程序中的问题判定条件、扣分和等级规则为初始业务口径。参考结果文件仅用于理解字段含义与表达形式；其中与当前程序冲突的规则示例不作为新版规则依据。发布后，这套规则作为不可变内容固化在对应板块配置版本中。

每条启用的接待质检问题规则必须配置维度。参考结果文件可以预填其中已经出现的问题维度，未覆盖的问题由管理员在发布新版前补齐；任一启用规则缺少维度时禁止发布。模型只返回问题 ID，维度始终从任务绑定版本的规则读取。

接待质检导入时，截图行的质检结果区必须处于一致状态：结果列全部为空时创建待分析记录；已有完整结果时将该行作为历史结果整行跳过，不把已有结果作为模型证据，也不覆盖；结果区只填写了一部分时阻止导入并提示冲突行，避免新旧结果混用。

接待质检导出只修改带聊天截图的记录行。没有图片的预留行保持原样，不补平台、不生成会话 ID，也不填写任何分析结果。
