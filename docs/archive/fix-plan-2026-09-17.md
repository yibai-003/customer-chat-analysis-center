# 当前项目修复清单

**日期：** 2026-09-17

## 目的

本文件记录当前工作区中已经实现但尚未完成最终验证或复核的内容。当前阶段只整理问题和验收要求，不直接进行业务开发。

## P0：先修复本地验证环境

### 1. 重建 Node 依赖

当前 `node_modules/tsx` 不完整：

- `node_modules/tsx/dist/cli.mjs` 缺失；
- `node_modules/tsx/dist/loader.mjs` 缺失；
- `npm ls tsx` 将其标记为 `invalid`。

因此全量测试中与跨进程、CLI、工作目录切换相关的测试会提前失败，`npm run db:check` 也无法启动。

处理要求：

```powershell
npm ci
npm test
npm run typecheck
npm run build
npm run db:check
```

若 `npm ci` 仍受证书或网络影响，应先记录安装失败原因，不要修改锁文件或绕过证书校验。

验收：

- `tsx` 的 `cli.mjs` 和 `loader.mjs` 存在；
- 全量测试不再因模块缺失失败；
- `npm run db:check` 能执行并返回数据库完整性正常。

## P1：完成未成交分析最终复核

相关文件：

- `docs/superpowers/specs/2026-09-15-lost-deal-analysis-design.md`
- `docs/superpowers/plans/2026-09-15-lost-deal-analysis.md`
- `.superpowers/sdd/2026-09-15-lost-deal-analysis/progress.md`

当前实现已经包含统一归因字段、原因知识库、证据校验和本地话术规则，但进度记录仍处于修复波次。

需要修复或确认：

1. `specificDemand` 的证据未充分落地时，公共字段必须保持为空，不能把未经证据支持的具体需求导出。
2. 客户原因知识库补齐 `示例表达` 元数据。
3. 客服问题知识库补齐 `改进方向` 和 `示例话术` 元数据。
4. 话术规则补齐：
   - 产品适配；
   - 回复不及时；
   - 未知组合；
   - 客户原因与客服原因的组合规则。
5. 模型请求预算不能破坏其他板块中超过当前预算数量的 AI 字段。
6. 增加真实 XLSX 导出回读测试，验证公共字段和内部字段的导出边界。

验收：

- 新记录最多一次视觉调用和一次文本归因调用；
- 原因只能来自启用的知识库标准名称；
- 证据不足时状态为待复核；
- 多值字段使用换行；
- 话术建议默认不产生额外模型调用；
- 历史结果不被自动重算；
- Excel 文件可以实际导出并回读。

## P1：完成接待流程质检最终复核

当前工作区包含迁移 009 至 013、统一质检分析、规则目录、证据编号和派生字段。

需要确认：

1. 售前、售后和混合场景的字段依赖是否都能完成执行。
2. 规则覆盖不完整时是否进入人工复核，而不是生成确定性等级。
3. 售前和售后问题的评分、等级、标签和导出字段是否保持一致。
4. `截图内容总结` 与 `统一质检分析` 的内部字段不会写入 Excel。
5. 接待质检迁移在已有数据库上可幂等执行，并保留历史字段运行记录。

验收：

- 接待质检专项测试通过；
- Excel 导出回读通过；
- 数据库迁移版本为 13；
- `/api/health` 和 `/api/ready` 正常；
- 旧板块行为无回归。

## P2：整理工作区改动

当前工作区有大量未提交改动，涉及 57 个修改或未跟踪文件，内容同时覆盖：

- 未成交分析；
- 接待流程质检；
- Excel 导入/导出；
- 知识库同步；
- 模型预算和错误分类；
- 前端详情展示。

在完成专项验证前，不应直接整体提交。后续应按功能边界拆分提交，至少区分：

1. 未成交分析；
2. 接待流程质检；
3. 兼容性及 Excel 导出；
4. 测试和文档。

## 当前验证基线

- 类型检查：已通过；
- 前端构建：已通过；
- `git diff --check`：已通过；
- 全量测试：445/450 通过；
- 失败测试：5 个，均受 `tsx` 依赖损坏影响；
- `npm run db:check`：被同一依赖问题阻断；
- 当前未完成业务代码未提交。

