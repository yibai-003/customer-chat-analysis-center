# 局域网真实业务验收记录（2026-09-18）

**范围：** 真实跨电脑导入、真实模型（千问百炼）解析、结果查看、导出、备份与独立恢复、容器重启持久化，以及正式模型池基线收敛。

**结论：** 内部试用版本已通过真实局域网导入、正式模型解析、结果查看、审核确认和导出闭环；只读/越权抽查与主机重启由用户确认，正式 HTTPS 与 CI 镜像升级/回滚仍未完成。所有结论以本文件“验证结果”表为准，不用桩模型或机器演练替代真实证据。

## 1. 环境与版本

| 项目 | 记录 |
| --- | --- |
| 入口 | `http://172.16.20.178:8788`（HTTP，遗留风险） |
| 运行主机 | 172.16.20.178（本机 Docker 预览实例，非固定发布主机） |
| 第二台工作站 | 用户确认 IP `192.168.230.1`（应用当前未自动采集客户端来源 IP，计算机名未采集） |
| 执行时间 | 2026-09-18 11:01 – 16:58（本地时间，UTC+8） |
| 运行镜像 | `customer-chat-analysis:lan-preview`，镜像 ID `sha256:62fcd7e50578f646497fff160a72c565b89514cdd119ddc3b46296c15fa9601c`；本地构建，未关联 CI 发布工件（追溯见工单 03） |
| 正式供应商 | 千问百炼 `dashscope.aliyuncs.com/compatible-mode/v1` |
| 正式默认模型 | 视觉 `qwen3-vl-plus`；文本 `qwen-plus`（2026-09-18 模型池收敛后生效） |
| 凭据 | API Key 仅保存在运行主机模型配置内；本记录与验收附件不含 Key、密码、Cookie 或加密密钥 |

## 2. 真实数据样本与数量

| 项目 | 记录 |
| --- | --- |
| 样本（脱敏名） | 未成交分析-脱敏.xlsx（板块 `lost-deal`） |
| 文件 SHA-256 / 大小 | `68d63c63f148d57a79fb665eb48893295cfa08497931987e5502cfe71c517020` / 2,362,976 字节（仅记录哈希，不附带原文件） |
| 任务数 | 导入 5 次（其中 4 次已由用户删除），当前保留 1 个任务：`2d53c2bf-ef5c-4c20-bd82-010de3b0f1c7` |
| 记录数 | 10 |
| 解析结果 | 10 条全部进入“待复核”，失败 0；部分字段完成、其余需人工复核 |
| 导出 | 4 次成功；最新导出 `2d53c2bf-…-客服解析结果.xlsx`，23 列 × 10 行，1,880,377 字节，SHA-256 `afc45688ef8e18c3541f2a5aefd29fd2c737e997f01202f851a91f9d85dc1f47` |

导出列核对（脱敏）：原始字段（日期、店铺、客服、客户ID、商品id、产品类、咨询产品、截图）、解析字段（截图内容总结、客户原因、客服原因、客户产品需求、其他需求、话术逻辑优化建议、问题点、改进方案、质检复核结果）。人员、最后审核时间、备注以及解析/复核状态仅保留在系统界面与数据库中，不写入业务导出文件。

## 3. 验证结果（四态）

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| 第二台电脑导入真实 Excel | 已通过 | 审计 `task.import/success`（13:25、13:31、13:43），样本哈希与 10 条记录 |
| 真实模型解析 | 已通过 | 千问模型成功用量事件（见第 4 节）；记录产出解析字段，10 条待复核、0 失败 |
| 结果查看（记录与原图） | 已通过 | 记录详情接口可读；原图 365,623 字节（sha16 `29cc13035742e02a`）在容器重启前后一致 |
| 导出内容 | 已通过 | 4 次 `task.export/success`；导出文件 23 列 × 10 行并与任务对应 |
| 审核人员复核 | 已通过（抽查证据） | 最新任务已有 4 条 `review.save`，均为 `reviewStatus=confirmed`；另有 7 条需复核、8 条待复核；审核账号已登录 |
| 只读人员权限与越权拒绝 | 用户确认 | 只读账号已登录并完成验收；越权结果由用户确认，当前服务端未单独记录 `access.denied` 事件 |
| 备份创建 | 已通过 | 应用内 `backup.create`（12:44，`verified:true`）；本次 CLI 备份 `files:16, references:12, verified:true` |
| 独立恢复与校验 | 已通过 | `restore` + `restore:verify`：5/5 检查通过（数据库 v17、11 个文件引用、知识目录哈希、53 个模型凭据可解密） |
| 主机重启 | 用户确认 | 用户确认主机重启及重启后业务核对；当前系统无法仅凭审计区分主机级重启与容器重启 |
| 正式 HTTPS / 内网 CA / DNS / 防火墙 | 未执行 | 本次为 HTTP 预览；按发布手册保留 |

## 4. 真实模型调用证据（2026-09-18 13:31–13:44）

| 模型 | 用途 | 结果 | 次数 | Tokens |
| --- | --- | --- | --- | --- |
| `qwen3-vl-plus` | 视觉 | 成功 | 30 | 101,578 |
| `qwen-plus` | 文本 | 成功 | 26 | 52,732 |
| `qwen3.7-max-2026-06-08` | 文本 | 成功 | 4 | 16,578 |
| `qwen-plus` | 文本 | 失败 → 冷却 → 切换备用模型 | 3 / 1 / 1 | 4,476（失败重试计量） |

说明：同一实例早前（11:07–13:25）存在桩模型调用，已在本记录中排除，不计入正式证据；桩模型与测试供应商已于 15:06 停用并移出路由池。

## 5. 模型池基线收敛（15:06）

- 收敛前备份：`full-2026-09-18T07-06-56-109Z-8e80a882-00f6-4f5a-b147-44befdb4c3c1`。
- 动作：`局域网测试视觉模型`、`局域网测试文本模型` 移出路由池并停用；测试供应商停用；默认设为 `qwen3-vl-plus`（vision）与 `qwen-plus`（text）。
- 复核：`/api/ready` 返回 200，vision/text 均 `passed`，阻塞项为空。
- 全部经公开 API 完成并进入审计，未直接修改数据库。

## 6. 未执行事项（保留待办）

1. 正式 HTTPS 入口（内网 CA、DNS、防火墙）与固定发布主机部署。
2. 使用通过 CI 的镜像工件完成升级/回滚预演，并记录镜像、提交 SHA 与工件清单（工单 03）。

## 7. 遗留风险

- 当前为 **HTTP 局域网入口**，无正式 TLS；会话与数据在局域网内无传输加密保护。
- 单机 SQLite 单实例架构，无多主机/高可用；解析吞吐受单主机与外部模型配额限制。
- 运行实例位于开发工作站 Docker Desktop，资源与稳定性不等同固定发布主机。
- 模型池含大量千问免费额度成员，配额与稳定性需持续观察；正式 HTTPS 未完成前不建议超出试用范围。

## 8. 脱敏声明

本记录仅含入口地址、主机标识、时间、镜像 ID、哈希、数量、状态与模型名称；不包含 API Key、密码、会话 Cookie、加密密钥、完整业务 Excel、完整图片或敏感路径。附件证据为哈希与接口摘要，生成时已确认不含上述敏感值。

## 9. 补充真实局域网任务（2026-09-18 16:35–16:58）

本次补充验收由用户确认在工作站 `192.168.230.1` 发起。由于当前应用审计事件没有记录客户端来源 IP，工作站 IP 属于人工验收记录，不是服务端自动采集的网络证据。

| 项目 | 记录 |
| --- | --- |
| 任务 | `32808632-2dce-478d-a7c5-14efa89fad8d`，板块 `refund`（退款分析） |
| 导入文件 | `退货分析_测试-未分析数据.xlsx` |
| 输入文件 SHA-256 / 大小 | `60B997F5DAE6D2BEF49F1A917EA373CE631236846FDF38EEA3650972736612FA` / 2,899,031 字节 |
| 导入结果 | 19 条记录、19 张图片处理成功、0 失败 |
| 解析结果 | 12 条解析完成、7 条需复核、0 条失败；审核状态为 4 条已确认、7 条需复核、8 条待复核 |
| 正式模型 | 千问百炼：`qwen-plus`、`qwen3-vl-plus`、`qwen3-vl-plus-2025-12-19`；调用事件均为成功 |
| 导出结果 | 2026-09-18 16:58 导出成功；2,331,036 字节；SHA-256 `DFC11F547712A8664A2EFA8AF29A726110406262B7BAD58C4C03CC7F1CD0A932`；`Sheet1` 为 19 条数据、22 列 |
| 可追溯审计 | `task.import`、`task.start_analysis`、`task.export` 均为 `success` |
