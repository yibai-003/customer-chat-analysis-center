PORT=8787
DATA_DIR=./data
DATABASE_PATH=./data/app.db
# Keep the existing ENCRYPTION_KEY when moving a database with saved model keys.
# Set an independent secret before production use; do not change it without key migration.
MAX_UPLOAD_MB=2048
ANALYSIS_CONCURRENCY=2
ANALYSIS_BATCH_SIZE=20
MIN_FREE_DISK_MB=512
BACKUP_INTERVAL_HOURS=24
BACKUP_RETENTION=7
CLEANUP_RETENTION_DAYS=14
LOG_MAX_MB=10
LOG_RETENTION=7
