# 复制为 deploy/.env 后填写真实值；本文件不包含任何真实密钥。

# 镜像：固定到发布版本，回滚时改回上一个标签。
APP_IMAGE=customer-chat-analysis:lan-2026-09-17

# 宿主机端口：应用只绑定 127.0.0.1，由内网反向代理/TLS 终止器对外提供服务。
APP_PORT=8787

# 必须提供：模型凭据加密密钥（独立值，勿与示例或旧默认值相同）。
ENCRYPTION_KEY=

# 必须提供：会话密钥（账号与会话单元启用后生效，预留强制项）。
SESSION_SECRET=

# 宿主机本地持久目录（不要放在网络共享盘）。
DEPLOY_DATA_DIR=./data
DEPLOY_KNOWLEDGE_DIR=./knowledge

# 资源限制。
APP_MEMORY_LIMIT=2g
APP_CPU_LIMIT=2.0

# 业务参数（可按主机调整）。
MAX_UPLOAD_MB=2048
ANALYSIS_CONCURRENCY=2
ANALYSIS_BATCH_SIZE=20
MIN_FREE_DISK_MB=512
BACKUP_INTERVAL_HOURS=24
BACKUP_RETENTION=7
CLEANUP_RETENTION_DAYS=14
LOG_MAX_MB=10
LOG_RETENTION=7
